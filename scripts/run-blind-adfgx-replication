#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${project_root}"

./scripts/generate-e008-blind-adfgx
./scripts/score-e008-blind-adfgx
exec ./scripts/unblind-e008-adfgx

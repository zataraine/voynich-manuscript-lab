#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${project_root}"

exec ./scripts/run python -m manuscript_lab.adfgx_stage_localization \
  --config config/experiments/E-007-adfgx-stage-localization.yaml

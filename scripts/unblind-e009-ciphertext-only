#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${project_root}"

exec ./scripts/run python -m manuscript_lab.ciphertext_structure_unblind \
  --config config/experiments/E-009-ciphertext-only-structure.yaml

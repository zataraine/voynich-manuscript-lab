#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${project_root}"

exec ./scripts/run python -m manuscript_lab.blind_adfgx_scorer \
  --config config/experiments/E-008-blind-adfgx-scorer.yaml

#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${project_root}"

exec ./scripts/run snakemake --snakefile workflow/external-signature-calibration.smk \
  --cores 12 --resources mem_mb=16384 --rerun-incomplete --printshellcmds

# Notebooks

Notebooks are for exploration and reviewed presentation, not the sole home of a
pipeline. Move reusable logic into `src/manuscript_lab/`, use stable IDs, and
write results through a configured run directory. Clear bulky cell outputs before
committing.

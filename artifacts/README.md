# Artifacts

Machine-generated diagnostics and immutable run directories. Contents are local
and ignored by Git. Promote reviewed conclusions to `reports/` and reference the
run ID rather than copying untracked results without provenance.

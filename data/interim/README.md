# Interim data

Reversible derivatives of raw sources: page renders, region crops, segmentation,
and parsed-but-unmodified transcription records. Each artifact needs parent IDs
and a transform/config version. Contents are ignored by Git.

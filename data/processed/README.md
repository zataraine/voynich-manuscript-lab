# Processed data

Rebuildable analysis tables, feature matrices, multimodal alignments, and split
assignments. Contents are ignored by Git; the creating config and run record are
tracked or preserved in the artifact store.

# Models

Downloaded weights live outside Git in the WSL Hugging Face/Torch caches. Before
using a model, copy `registry.example.yaml` to a tracked project registry entry
and record its exact revision, license, size, purpose, safety settings, and review
status. `trust_remote_code` remains false unless explicitly approved and audited.
